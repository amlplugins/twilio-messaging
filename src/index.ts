/**
 * @amlplugins/twilio-messaging
 *
 * Thin re-export of twilio (CommonJS `export =` SDK).
 *
 * Twilio Programmable Messaging — SMS, MMS, WhatsApp via Messaging Services.
 */

import _sdk from "twilio";
export { _sdk as sdk };
export default _sdk;
